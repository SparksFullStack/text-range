function mainFunc() {
    const range = document.createRange();
    const findingText = document.getElementsByClassName('finding-text')[0].innerHTML.split(' ');
    console.log('finding', findingText);
}